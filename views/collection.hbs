<section id="product-section">
    <div class="title">
        <h2>Products</h2>
    </div>
    {{#each items}}
    <div class="products">
        {{> myPartial}}
    </div>
    {{/each}}
</section>
{{> footer}}
<h1> {{title}} </h1>