<section id="product-section">
    <div class="title">
        <h2>Products</h2>
    </div>
    <div class="products">
        {{#each items}}
        {{> myPartial}}
        {{/each}}
    </div>
</section>
{{> footer}}